{
  "name": "coding-runtime",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Batteries-included base runtime for language-operator harness adapters: OS layer, terminal surface, and the /etc/agent/config.yaml ETL.",
  "license": "Apache-2.0",
  "bin": { "coding-runtime": "./src/cli.mjs" },
  "scripts": {
    "test": "node --test \"test/**/*.test.mjs\""
  },
  "dependencies": {
    "@xterm/addon-clipboard": "^0.2.0",
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.5.0",
    "node-pty": "^1.0.0",
    "ws": "^8.18.0",
    "yaml": "^2.8.3"
  }
}
